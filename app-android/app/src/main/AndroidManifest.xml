<?xml version="1.0" encoding="utf-8"?>
<!--
    佛乐 · 净土法音 —— 安卓离线应用

    壳与讲记正文随包出厂，挂在 WebViewAssetLoader 的自有域 https://foyue.org 下，
    与线上同源，故站点代码不必为 APP 改写。详见 MainActivity 的说明。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 音频流、问道与莲号接口、目录更新；正文阅读与念佛计数全程不联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 「有新版直接更新」要唤起系统安装器 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- 后台恭听：前台服务保住进程，媒体会话提供锁屏与通知栏控制 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- API 33 起通知要运行时授权。拒绝也照常播，只是没有那块控制面板 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/launcherName"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:enableOnBackInvokedCallback="true"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            后台恭听服务。

            foregroundServiceType 从 API 29 起就该声明，API 34 起是强制的；
            少了它，Android 14 上一 startForeground 就抛
            MissingForegroundServiceTypeException，表现为「一播就闪退」。

            MEDIA_BUTTON 过滤器是给 MediaButtonReceiver 用的：耳机线控与蓝牙的
            按键先落到那个 receiver，再由它转投到本服务；服务这边没有这条过滤器，
            androidx 会直接拒绝转投。
        -->
        <service
            android:name=".MediaService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>

        <receiver
            android:name="androidx.media.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- 下载好的新版安装包要经 FileProvider 交给系统安装器，不能直接递 file:// -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>
    </application>
</manifest>
