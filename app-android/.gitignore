# 签名密钥与密码：绝不进仓库（丢失＝这个 APP 永远无法更新，泄露＝可被冒名发版）
keystore/

# 构建产物
build/
app/build/
.gradle/
*.apk
*.aab
*.idsig

# 站点内容副本：由 scripts/build-app-assets.py 从 public/ 生成（约 20MB、数百个文件）。
# 源头在 public/，这里只是打包中转；进仓库既冗余，又会把每次文库重建变成上万行 diff。
app/src/main/assets/

# 本机环境
local.properties
.DS_Store
