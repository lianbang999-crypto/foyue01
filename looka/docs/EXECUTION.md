# Looka · 执行清单（唯一施工依据）

> **这份文件的存在理由**：`FEATURE-PLAN.md` 已经 3579 行、43 节，
> 方案写得越全，越容易「写过了 = 以为做过了」。
> 本文件把**全部未完成项**收拢成一张可勾选的表，按依赖顺序排好。
>
> **规矩**：
> 1. 只在这里勾 `[x]`，勾了就代表**已实测通过**，不是"代码写完了"
>    - `[~]` = **代码完成、编译与部署通过，但功能行为尚未实测**（多为需真机/登录态的项）
>    - 看到 `[~]` 就去对应的「实测待办」表里验，验过了才改成 `[x]`
> 2. 每项都标了出处（§N），细节去计划书查，不在这里重复
> 3. 新的想法先写进 `FEATURE-PLAN.md`，**确认要做**才登记到这里
> 4. 发版前把本文件从头扫一遍
>
> 最后更新：2026-08-22 · v1.7.1 (15) + **后台 v2 上线**（分站分页/用户画像/留存/信标/Ko-fi钩子/爱发电新令牌）。77 项已勾（+双引擎/Ko-fi启用/批B三项）

---

## P0 · 只需要你点几下（不用写代码，但卡着后面）

| | 项 | 出处 | 状态 |
|---|---|---|---|
| P0-1 | ✅ 已做（用户 8-24 确认）。§84 查证：测试推送 4 次全部抵达正确 secret 路径 = URL 对了；但那 4 次都是爱发电示例假单号，**真实订单尚未走过 webhook**（唯一一笔是 cron 对账捞的）—— 下笔真单进来时查 `afdian_hook` 埋点才算验完。原文：**爱发电后台改 Webhook URL** → `https://looka.foyue.org/api/pay/afdian/6091ea24b659e90a0eeaa53992d6448b`，然后点「发送测试」<br>（接口已上线，实测返回 `{"ec":200,"em":""}`） | §33 §41 | [ ] |
| P0-2 | **Ko-fi `/tiers` 确认 Membership 档位真的建好了**（上次给的是 `/commissions`，海外用户点过去可能是空页） | §32 §36 | [x] |
| P0-3 | **DMARC 记录**：`v=DMARC1; p=none; rua=mailto:looka01@qq.com`（影响 QQ/163 收信率） | §36 | [x] |
| P0-4 | **keystore 异地备份 #2**：`.secrets.txt` 存进密码管理器，**密码必须与 keystore 分开存** | §36 | [x] |
| P0-5 | ✅ 订单号自助认领路径**此前已实测通过**（用户 8-24 确认）。原先那段「兑换码接不进管线」的分析基于一个错误样本，**已整段撤回**（记录见 §84 顶部） | [x] |
| P0-6 | ~~确认主站形态~~ **已确认：Worker**。后台以独立 Worker 挂 route `foyue.org/admin*`（比主站 custom_domain 更具体，优先命中），主站零改动 | §42 | [x] |
| P0-7 | ~~开通 Cloudflare Access~~ **用户 2026-08-22 决定：暂不开，等有稳定用户量再开**。后台已用强口令+限流顶上 | §42 §43 | [x] |

---

## P1 · 埋点（🔴 唯一有时效性的一批，晚一天少一天数据）

> 出处：§43 第 0 期。**建议排在所有开发任务最前面。**

| | 项 | 状态 |
|---|---|---|
| P1-1 | `foyue-db` 建表：`events` / `daily_stats` / `ip_salts`（DDL 见 §43 0.1） | [x] |
| P1-2 | `rixing-db.users` 加列：`last_seen_at INTEGER` / `reg_site TEXT` | [x] |
| P1-3 | Looka `wrangler.jsonc` 增加 D1 绑定 `STATS → foyue-db` | [x] |
| P1-4 | worker.js 写入 `track()` / `dailySalt()` / `sha256hex()` 三个辅助函数 | [x] |
| P1-5 | 接入点 a：`/dl/looka-latest.apk` → `apk_download`（🔴 当前完全空白） | [x] |
| P1-6 | 接入点 b/c：注册 / 登录 → `register` / `login` + 写 `reg_site` | [x] |
| P1-7 | 接入点 d：`getUser()` 后当日首次 → `app_open` + 更新 `last_seen_at`（每天最多写一次） | [x] |
| P1-8 | 接入点 e/f/g：`pay` / `ai_chat` / `crash` | [x] |
| P1-9 | 接入点 h：网页未登录首页 → `POST /api/ev` 记 `landing_view` | [x] |
| P1-10 | 每日 Cron：汇总 `daily_stats` + 清 90 天前 events + 清 30 天前 salts | [x] |
| P1-11 | 🔴 **隐私政策补「运行数据」条款**（文案见 §43 0.6） | [x] |
| P1-12 | 全部 `track()` 用 `ctx.waitUntil()` 调用，确认不占用户等待时间 | [x] |

---

## P2 · 修 bug（根因都已定位，改法都已写好）

### P2-A 订阅闭环最后一米（🔴 正在漏钱/漏信任）

> 出处：§39 + §41。**这三件事是同一件事的三个面，必须一批做完。**

| | 项 | 状态 |
|---|---|---|
| P2-A1 | `Prefs` 增加 `planExpiry` / `planSyncedAt`；`isPro` 本地即可判过期 | [x] |
| P2-A2 | 新建 `PlanState`（Compose State），**收编全部 5 个读取口** | [x] |
| P2-A3 | 🔴 `AccountScreen.kt:312` 改为 `apply()` 落盘（**根因那一行**：读到真值却不记下来） | [x] |
| P2-A4 | 🔴 认领成功后 `apply()`（App `ExtraScreens:280` + 网页 `mClaim` 都漏了 refresh） | [x] |
| P2-A5 | 新增 `onResume` 刷新（>5 分钟节流）—— **让自动开通看起来是即时的** | [x] |
| P2-A6 | `/api/sync` 响应带 `plan` + `plan_expiry`（免费获得持续刷新通道） | [x] |
| P2-A7 | 网页 `visibilitychange` 监听 + `isPro` 统一判定 | [x] |
| P2-A8 | `wrangler.jsonc` Cron 加 `*/5 * * * *` 拉单对账，`scheduled` 按表达式分流<br>（**即使 Webhook 永不配置，最慢 5 分钟也自动开通**） | [x] |
| P2-A9 | 订阅页「等待付款」三态卡片：等待中 / ✅已开通 / 超时后才给认领入口 | [x] |
| P2-A10 | 认领入口从「更多」页**撤下**，只在超时后出现（平时显示 = 暗示流程会坏） | [x] |
| P2-A11 | 更多页加一行「升级 Pro · 12元/月」直达付款（入口 3 层 → 1 层） | [x] |
| P2-A12 | 兑换码改文案「有兑换码？」收进订阅页折叠区（保留送人场景） | [x] |

### P2-B 月历显示（v1.6.0 我引入的回归）

> 出处：§38 ②。两端同改。

| | 项 | 状态 |
|---|---|---|
| P2-B1 | 白/灰改**按月份奇偶交替**，与滚动位置无关（`inMonth` 概念在连续滚动里本就不成立） | [x] |
| P2-B2 | 月份水印改画在**格子之上**、低透明度、缩到一行装得下（现在被格子底色盖住，只漏出顶部 30%） | [x] |
| P2-B3 | 数据窗口 ±1 月 → **±2 月**，并改用**视口中心日**做锚（少一层延迟传导） | [x] |
| P2-B4 | 网页端同步：去掉 `.week-row .day-cell{background:transparent}`（它把 `.dim`/`.sel` 一起干掉了） | [x] |

### P2-C 网页端 4 个可证 bug

> 出处：§40。

| | 项 | 状态 |
|---|---|---|
| P2-C1 | **W1** `.frow-inline{flex-wrap:wrap}` + 子项 `min-width:140px`（四个字段挤在一行，时间框被裁成 `09:0`） | [x] |
| P2-C2 | **W2** 弹窗折叠：只留 标题/日期/全天，其余进「显示详细设置」——**与 App 同一套措辞** | [x] |
| P2-C3 | **W3** flex 撑满 + `100dvh`，顶栏/底栏高度用 JS 实测写进 CSS 变量（弃用 `calc(100vh - 210px)` 这个拍脑袋常数） | [x] |
| P2-C4 | **W4** 同 P2-B4（CSS 特异度覆盖事故） | [x] |

### P2-D AI 提醒（最影响信任：说了会提醒，结果没响）

> 出处：§38 ①。根因是 AI 不理解「提醒我」，只会「建日程」。

| | 项 | 状态 |
|---|---|---|
| P2-D1 | `AiAction` 增加 `remindAtMin` / `remindMinBefore` 字段 | [x] |
| P2-D2 | 提示词教会区分：「10:55 开会」= 日程；**「10:55 提醒我」= 提醒时刻就是 10:55**（`minutesBefore=0`） | [x] |
| P2-D3 | AI 回复必须报出提醒时刻：「✅ 已添加：10:55 吃药 · **10:55 提醒你**」 | [x] |
| P2-D4 | 🔴 提醒时间已过去要**明说**，不再静默 `continue`（`NotifyScheduler.kt:106`） | [x] |
| P2-D5 | 提醒时刻已过但日程在未来 → 自动退化成「日程开始时提醒」 | [x] |

### P2-E AI 高级模型可观测

> 出处：§38 ③。服务端是好的，问题是坏了查不出原因。

| | 项 | 状态 |
|---|---|---|
| P2-E1 | 服务端 `console.log('premium fail', model, status, err)` | [x] |
| P2-E2 | **失败重试一次再回落**（实测存在 12 秒长尾，一次重试大概率救回） | [x] |
| P2-E3 | OpenRouter 调用加 `AbortSignal.timeout(25000)` | [x] |
| P2-E4 | `fell_back.detail` 透传到客户端，气泡下方显示真实原因 | [x] |
| P2-E5 | `/api/admin/health` 增加 `premium_fail_24h` | [x] |

---

## P3 · 管理后台（三期全做）

> 出处：§42 §43。P1 埋点是它的地基，必须先完成。

### P3-1 期：能看

| | 项 | 状态 |
|---|---|---|
| P3-1-1 | 新建 `foyue-admin/` 项目骨架（wrangler.jsonc + src/worker.js + public/index.html） | [x] |
| P3-1-2 | 路由 `foyue.org/admin*`；D1 绑定 STATS/AUTH/LOOKA/SHOP | [x] |
| P3-1-3 | 鉴权（**方案变更**：用户决定暂不开 CF Access，等有稳定用户量再加）：FOYUE_ADMIN_KEY 强口令 + 登录失败限流（8 次锁 1 小时）+ 写操作审计。将来上 Access 时代码无需改动 | [x] |
| P3-1-4 | `GET /admin/api/overview`（今日·昨日·7日 + 30 日漏斗 + 本月收入） | [x] |
| P3-1-5 | `GET /admin/api/trend`（走 daily_stats） | [x] |
| P3-1-6 | `GET /admin/api/users`（🔴 **只返回元数据，永不含笔记/日记/日程正文**） | [x] |
| P3-1-7 | `GET /admin/api/subs`（流水 / 未认领 / 即将到期） | [x] |
| P3-1-8 | `GET /admin/api/health`（AI 失败率·回落·崩溃 Top·限流） | [x] |
| P3-1-9 | `GET /admin/api/sites`（各子站对比） | [x] |
| P3-1-10 | 单页看板 UI：总览 / 用户 / 订阅 / 健康 / 子站 五个标签（原生 JS，不引框架） | [x] |

### P3-2 期：能做事

| | 项 | 状态 |
|---|---|---|
| P3-2-1 | `admin_audit` 表 | [x] |
| P3-2-2 | 写操作：补开 Pro（调 Looka `/api/admin/grant`，业务规则留在归属站） | [x] |
| P3-2-3 | 写操作：生成兑换码 / 邀请码（复用 gencode） | [x] |
| P3-2-4 | 写操作：手动绑定未认领订单 | [x] |
| P3-2-5 | 写操作：封禁账号（`users.banned_at` + `getUser()` 拦截） | [x] |
| P3-2-6 | 每个写操作二次确认 + 记审计 | [x] |
| P3-2-7 | 接入自知录（admin 已可读账号总库；**该站自己的埋点未接**，待下批） | [x] |
| P3-2-8 | 接入流通处（admin 已绑 SHOP 并显示订单/客服量；**该站埋点未接**） | [x] |
| P3-2-9 | 接入播经台 / 文钞（流量场景少，暂缓 —— admin 侧就绪，接入只需各加一条 binding + track） | [ ] |
| P3-2-10 | 异常告警邮件（未认领订单>0 / 崩溃激增 / AI失败率>10% / 注册归零） | [x] |

### P3-v2 后台升级（§44，2026-08-22 用户要求"完整数据+分站+了解用户"）

| | 项 | 状态 |
|---|---|---|
| V2-1 | 子站分页 ×7（趋势图/时段分布/事件/设备/来源） | [x] |
| V2-2 | 用户画像钻取（活跃天数+行为流水，红线：永不含内容） | [x] |
| V2-3 | 留存页（次日/7日/周活） | [x] |
| V2-4 | 公开信标端点（白名单+限流+CORS，实测通） | [x] |
| V2-5 | Ko-fi Webhook（邮箱自动匹配开通；待用户提供验证令牌才启用开通） | [x] |
| V2-6 | 爱发电新令牌换存 + Worker 实测 ec:200 | [x] |
| V2-7 | 🔴 用户贴信标到 主站/文钞/game/播经台（脚本见 §44；主站仓库已分叉 ahead14/behind15，见 §45 五） | [ ] |
| V2-8 | 用户提供 Ko-fi 验证令牌 → 存 KOFI_VERIFY_TOKEN | [x] |
| V2-9 | Ko-fi 未认领单的后台绑定（bind-order 泛化渠道） | [ ] |

---

## P4 · 对齐 Lifebear（功能量，排在 bug 之后）

> 出处：§32 §37 §40。

| | 项 | 状态 |
|---|---|---|
| P4-1 | 日面板头部与 App 一致：日期 + 农历 + **写日记** + ＋日程（网页缺"写日记"） | [x] |
| P4-2 | 网页编辑器**四模式切换**（日程/任务/表情/AI）—— Lifebear 节奏的核心 | [ ] |
| P4-3 | 网页周视图 / 日视图（`calView` 目前 0 命中） | [ ] |
| P4-4 | 网页分类管理 + 日历设置两个页面 | [ ] |
| P4-5a | 网页未来 7 天 | [~] |
| P4-5b | 网页已完成清单（清单归档 / 恢复 / 已完成清单页） | [ ] |
| P4-6 | 网页拖拽排序 / 长按（右键）菜单 | [ ] |
| P4-7 | 网页备份**恢复**（现在只有导出） | [x] |
| P4-8 | ラベル 标签体系（与清单正交，多对多）—— Pro 权益候选 | [ ] |
| P4-9 | App 待办滑动手势（F6，品类标配） | [ ] |
| P4-10 | 印章商店 / 主题商店 + 「先免费领一个」拉新钩子 | [ ] |

---

## P5 · 把「两端必须同步」从口号变成机制

> 出处：§40 第五层。**不做这个，今天补完下个月又散。**

| | 项 | 状态 |
|---|---|---|
| P5-1 | 🔴 **设置上云**：`KINDS` 增加 `settings`，把 `weekStartMonday`/`showLunar`/`holidayMask`/`showDoneTasks` 变成同步实体<br>（现在 App 改周起始，网页永远还是周一 —— 两端看到不同的日历） | [x] |
| P5-2 | 交互文案纳入 i18n 单一真源，两端引用同一 key（措辞不一致就不可能发生） | [x] |
| P5-3 | `UI-SPEC.md`（现 33 行）扩成**两端对照表**，发版逐行打勾 | [x] |

---

## P6 · 上线前必改（内测值不能带上正式版）

| | 项 | 出处 | 状态 |
|---|---|---|---|
| P6-1 | 🔴 `TRIAL_DAYS` **365 → 30** —— **F 批已改并部署**（与 F-4 重复登记） | §36 | [x] |
| P6-2 | 首页介绍页措辞改准 —— **不能承诺网页端还没有的能力**（现在按同等能力宣传） | §40 | [ ] |
| P6-3 | `REGISTER_MODE` 按上线策略确认（open / invite / closed） | — | [ ] |
| P6-4 | 全站扫一遍「内测期间免费」类文案，与实际计费一致 | §22 | [ ] |

---

## P7 · 待你拍板（不定就排不了期）

| | 决策 | 我的建议 | 状态 |
|---|---|---|---|
| P7-1 | 印章模式点开后，是否跟 Lifebear 一样**退回月历**（所见即所贴）？<br>我们现在留在编辑器里填表单，好处是能顺手绑定日程（Lifebear 没有这功能） | 倾向保留我们的做法 | [x] ← §75 定案：贴纸面回月历（所见即所贴），绑定日程能力保留 |
| P7-2 | 小鹿 AI 按钮是否留在日历顶栏 | **建议留**（Lifebear 没 AI，无此约束） | [ ] |
| P7-3 | 印章/主题定价：鹿角计价 vs 直接标价 | Lifebear 直接标日元 | [ ] |
| P7-4 | 是否上广告条（Lifebear 把「去广告」当独立商品卖） | 与「让利于用户」有张力，倾向不上 | [ ] |
| P7-5 | App 里的支付入口算不算「宣传爱发电」（你说 App 不宣传爱发电） | 我理解要去掉的是**广告位**、不是**付款通道** | [ ] |

---

---

## P8 · 主线 A：AI 从「录入器」变「助理」（§48 一，2026-08-22）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| A1-1 | 动作协议扩容：`update_*` / `delete_*` / `query_events` | 服务端 prompt + `AiActions` 解析器**同步**改 | [~] |
| A1-2 | 对话上下文注入「今天±7天日程摘要（id+标题+时间）」 | AI 靠 id 精确定位，**禁止靠标题模糊匹配删** | [~] |
| A1-3 | `execActions` 加 update/delete 分支 | `LookaViewModel.kt:707` | [~] |
| A1-4 | 删除一律弹二次确认卡（列出要删的 N 条） | 删错不可逆 | [~] |
| A1-5 | 网页端同步实现 | 守「网页和 APP 同步」 | [~] |
| A1-6 | 🩹 重复记录查重清理入口 | 修用户已产生的重复数据 | [~] |
| A2-1 | 新建 `looka_alarm` 渠道（USAGE_ALARM + bypassDnd + HIGH） | ⚠️ 老渠道属性不可变，**必须新 id** | [~] |
| A2-2 | 通知加 CATEGORY_ALARM + setOngoing + setFullScreenIntent | | [~] |
| A2-3 | `AlarmActivity`：MediaPlayer 循环 + 振动 + 停止/再响5分钟 | | [~] |
| A2-4 | 申请 `USE_FULL_SCREEN_INTENT` 权限（A14+） | | [~] |
| A2-5 | 编辑页加「⏰ 当成闹钟（会持续响）」开关，与普通提醒分开 | | [~] |
| A2-6 | 自检页加第 5 项（闹钟渠道静音 / 全屏意图权限） | | [~] |
| A3 | 批量确认卡 + 10 秒内一键撤销（按批次 id 回滚） | | [~] |
| A4 | `query_events` 查询类动作 | | [~] |
| A5 | 📸 截图识别建日程（走多模态，无需第三方授权） | | [ ] |
| A6 | 🔗 深链跳转库（携程/12306/美团/滴滴，**只跳不下单**） | | [ ] |

**A2 验收（必须实测）**：手机静音仍响 / 锁屏弹全屏 / 不按停止就持续响 / 贪睡有效 / 普通提醒仍只响一声

---

## P9 · 主线 B：主题系统 v2（§48 二）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| B1 | 🔴 主题上云（`KINDS` 8 → 9，加 `theme`） | 不做这个后面全白做 | [~] |
| B2 | `DeerTheme` 6 → 11 字段（texture/decor/banner/fabStyle/ink） | ⚠️ 新字段给默认值，老主题反序列化不能炸 | [~] |
| B3 | 小鹿 `ic_deer_badge.xml` 主色 tint + 九色光环随主题 | 17 条 path，色值可寻址 | [~] |
| B4 | 主题页重构成调色台（色轮 + 纸色滑杆 + 纹理/装饰横滑 + 实时预览） | 对齐 Lifebear | [ ] |
| B5 | 素材库：纹理 8~12 / 装饰 8~12 组 / 横幅 4~6 | **美术工作量，可并行** | [ ] |
| B6 | 端上取色（androidx.palette）+ AI 选配命名 → 3 套候选 | 图片不出设备；< ¥0.001/次 | [~] |
| B6-0 | 前置：引入 `androidx.palette` 依赖 | v1.8.1 已引入 | [x] |

---

## P10 · 主线 C + 商业化文档（§48 三/五）

| | 项 | 状态 |
|---|---|---|
| C-1 | ⚠️ FREE/PRO 定档拍板（建议：装扮墙 + 双档 ¥6/¥12） | [ ] |
| C-2 | 低价档「小鹿的朋友 ¥6/月」在爱发电/Ko-fi 建方案 | [ ] |
| BP-1 | 商业计划书 `docs/BUSINESS-PLAN.md` | [x] |
| BP-2 | ⚠️ 竞品定价/规模实时复核（见 BP 13.1 五项） | [ ] |
| BP-3 | 软著 + 备案（国内上架前置） | [ ] |
| BP-4 | 🔴 **`foyue.org` 可能根本不能备案**：`.org` 后缀未获工信部批复（阿里云/腾讯云备案文档均如此说）。若属实，§0 备选①「朋友公司主体备案后上国内 CDN」是死路 —— 卡的是域名后缀不是主体，需另备可备案的 `.com/.cn` 技术域名。Cloudflare China Network 也要求 apex 有 ICP + Enterprise 订阅，同样堵死。**先核实，再决定要不要现在囤域名**（§81） | [ ] |
| BP-5 | 🔴 **《AI 生成合成内容标识办法》2025-09-01 已施行**：覆盖 AI 生成的文本/图片/音频/视频，要求显式 + 隐式标识。⚠️ §82 更正：真 AI 生成**只有小鹿对话一处**（主题是本地取色、表情包未上线）。**显式标识已加**（聊天页一行，双端）；剩隐式标识与上架材料，留到表情包生成上线时一并办（§82 三） | [ ] |

---

---

## P11 · FREE/PRO 分立与文案 v2（§50，2026-08-22 定稿）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| F-1 | `FREE_AI_RPD` 环境变量（默认 10），**不要写死** | 免费额度政策会变，要能一键下调 | [x] |
| F-2 | AI 鉴权分档：free 走日限额 / pro 不限次 | `worker.js:916` 附近 | [x] |
| F-3 | 额度用完的友好文案（小鹿语气，非「请升级」） | **App + 网页双端** | [~] |
| F-4 | 🔴 `TRIAL_DAYS` 365 → 30（与 P6-1 同一条） | 忘了就是白送一年 | [~] |
| F-5 | 爱发电文案 v2 贴进后台 | 已写好 `STORE-COPY.md` | [ ] |
| F-6 | Ko-fi 文案 v2 贴进后台（英文） | **两处都要改，不能只改一处** | [ ] |
| F-7 | 降级说明页（「你保留了」在左，「暂时用不了」在右） | **App + 网页双端** | [~] |
| F-8 | 到期前 7 / 3 / 1 天提醒 | 服务端 | [~] |
| F-9 | 主题分享：FREE 可接收应用，不可生成 | 依赖 B1 主题上云 | [ ] |
| F-10 | ⚠️ 送 Pro 大规模推广**推迟到 B 主线完成后** | 现在 Pro 几乎是空的，祛魅只有一次机会 | [ ] |

**底线（不可动，§50 二）**：核心手帐功能不阉割 · 提醒闹钟免费 · 数据导出免费

---

---

## P12 · §52/§53 探讨产出（**等用户逐条拍板后执行**，2026-08-22）

> 这一段全部是「已探讨、未开工」。**执行时逐条对照，做完打 [x]，没做的最后要说明为什么。**

### 更懂你的小鹿（§52 一）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| D1 | `deer_memory` 实体（facts + patterns），随云同步 | 注入提示词仅几十 tokens | [~] |
| D2 | AI 动作 `remember`：对话中记下用户说的偏好 | 存下后一行小字「记住啦 🦌」，不弹窗 | [~] |
| D3 | 端上算 patterns（会议时段分布 / 任务推迟次数 / 本月日记数） | **只传结论，不传原文** | [ ] |
| D4 | 🔴 「小鹿记住了什么」页面：可看、可逐条删 | 看不见的记忆是监视 | [~] |
| D5 | ⚠️ 情绪/日记层**不做** | `aiDiaryUpload` 默认 false 是隐私底线 | — |

### 周视图（§52 二）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| W1 | 🔴 打开位置：`min(最早日程-1h, 现在-1h)`，都没有才回落 7:30 | 现写死 7.5h，一行改动收益最大 | [~] |
| W2 | 全天区最多 2 行 + 「还有 N 个」 | 与月视图一致 | [~] |
| W3 | 空白时段折叠成 24dp 细带（点击展开） | 0:00–6:00 白占 360dp | [ ] |
| W4 | 双指缩放 `HOUR_DP`（40/60/100 三档，记住选择） | | [ ] |
| W5 | 窄列策略：色块+首字，或加「3 日视图」档 | 7 列在手机上每列仅 ~50dp | [ ] |

### AI 界面对齐全站（§52 三）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| U1 | 消息进场 `fadeIn(180)+slideInVertically`（用户从右/小鹿从左） | AI 页现在 **0 处动画** | [~] |
| U2 | 圆角回归全站 10dp 系（输入框 22dp 胶囊保留） | 现用 14/16/22 | [ ] |
| U3 | 思考态换成三点跳动 / 小鹿呼吸（`tween(1600)` 全站已有） | | [~] |
| U4 | 发送键 `spring` 缩放反馈 | 对齐 TaskListScreens | [~] |
| U5 | ROLE_ACTION 改成全站卡片样式（浅底+10dp+左图标） | | [~] |
| U6 | 批量确认卡用 `AnimatedVisibility` 入场 | | [~] |
| U7 | 录音中麦克风脉动（App 端现无反馈） | | [ ] |

### 模型统一与打字机（§53）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| M0 | ⚠️ **拍板统一用哪个模型** | 建议 Qwen 档；**先跑探针实测价格与 Worker 连通性** | [x] |
| M1~M4 | 删双端档位 UI + 服务端 tier/鹿角分支 + 气泡模型标注 | 依赖 M0 | [~] |
| M5 | 🔴 §50 权益表 + STORE-COPY 中英文删「更聪明」 | **两个后台也要改** | [x] |
| M6 | premium 分支注释保留（可恢复），不删代码 | | [~] |
| M7 | 鹿角存在意义重新交代（AI 不再扣费后只剩表情包一个出口） | | [~] |
| T1 | 真流式 SSE（Worker+App+网页） | 排在 U1~U7 之后 | [~] |
| T2 | 🔴 流式时缓冲 JSON，绝不露裸 JSON 给用户 | 线上踩过的坑 | [~] |
| T3 | 动作卡片明确**不做**打字机 | | [~] |
| T4 | ❌ 假打字机**不做** —— 负收益（延长等待） | 决定，非待办 | — |

---

---

## P13 · §54 实图诊断与崩溃排查（**等用户拍板后执行**，2026-08-22）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| C1 | 🔴 启动补传 `last_crash.txt` → `/api/crash` → 删文件 | **上报链路一直是断的：接口有、写文件有、就是没人调用** | [~] |
| C2 | 关于页「发送崩溃日志」入口 | 私有目录用户自己拿不到 | [~] |
| C3 | 崩溃上传脱敏（不带账号/日程标题） | | [~] |
| C4 | admin 崩溃列表页 | LOOKA binding 已就绪 | [x] |
| C5 | premium 重试 2×30s → 1×20s | 最坏 60 秒无响应，像卡死 | [ ] |
| K1 | 🔴 `layoutLanes` 宽度改「向右扩展」 | 一根长日程把整段串成一簇 → 全列压成 17dp 不可读 | [~] |
| K2 | 🔴 AI 建日程带分类 / 「未分类」默认色改掉 | 回落色与未分类色都是 #9AA0A6 → AI 建的必然全灰 | [~] |
| K3 | 窄列隐藏时间行 | 「09:00-1」截断 | [~] |
| G0 | ⚠️ **拍板：鹿角取代次数（不能与 FREE_AI_RPD 并存）** | 并存必然出现「有鹿角但说次数用完」 | [~] |
| G0b | `ANTLER.cost` 统一 `chat: 1` | 现 standard=0，统一模型后扣 0 空转 | [~] |
| G1 | 更多页鹿角余额（安静一行，非角标） | | [~] |
| G2 | 鹿角明细页（`antler_ledger` 已有数据） | | [~] |
| G3 | 每日到账轻提示（不弹窗不特效） | | [~] |
| G4 | 余额<10 时才在输入框旁提示消耗 | | [~] |
| G5 | 余额不足引导文案 + Pro 入口 | | [~] |
| G6 | 发放改「登录到账」（现为惰性月度） | 淡化打卡：不显示连续天数、断签无惩罚 | [~] |
| G7 | 余额 0 保底 3 次/天 | 0 次 = 用户再也想不起这功能 | [~] |

**红线（不可碰）**：写日记 / 建日程 / 贴表情 / 同步 / 导出 / 闹钟 —— **永不扣鹿角**

---

---

## P14 · §55 鹿角定价（**等用户拍板**，2026-08-22）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| P1 | ⚠️ 拍板价目：100枚/¥6 · 300枚/¥15 · 1000枚/¥40 | Pro ¥12 给约1500枚 → 单买¥60，**Pro 便宜 5 倍** | [x] |
| P2 | ⚠️ 拍板消耗：对话1 / 主题2 / 截图3 / 表情包20 | 手帐核心功能永 0（红线） | [x] |
| P3 | 🔴 `ANTLER.cost` 改按动作计价（现按模型档位） | 统一 Qwen 后 standard=0 → 扣 0 空转 | [~] |
| P4 | 鹿角页显示折算价，**不开购买入口** | 定价 ≠ 开卖；锚定不需真卖就生效 | [~] |
| P5 | 🔴 邀请奖励双方各 +100 枚 | 成本≈0、感知¥6、不挤占订阅；补 BP 第8章获客洞 | [~] |
| P6 | 里程碑奖励（昵称/首篇日记/满一周 各+20） | **里程碑，不是签到** | [~] |
| P7 | ⏸ 开卖（两平台建一次性商品 + 入 `paid` 桶） | 7 个用户没规模，推后 | [ ] |
| P8 | 文案改「用掉/还有」，禁用「扣费/余额不足/充值」 | 鹿角是够用的额度，不是要攒的资产 | [~] |

**保留不动**：双桶记账（`granted` 优先扣 / `paid` 永不过期）—— 以后开卖时靠它挡「我买的被清零了」客诉

---

---

## P15 · §56（语音下线已执行；其余等拍板，2026-08-22）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| S1 | 语音输入全删（App+网页+Manifest queries） | 用户指令；零残留，随 v1.9.1 发布 | [x] |
| S2 | ⚠️ 周视图未分类配色：散列色 → 主题色柔和变体 | 散列色=假分类信号（用户点破，我认账） | [~] |
| S3 | ⚠️ 更多页 16→8 整合（订阅·鹿角合一 / 日历设置合一 / 数据维护合一 / 关于收编语言与更新） | 连着 P7-2 顶栏小鹿去留 | [~] |
| S4 | 拍照生成主题（需 CAMERA 权限） | 建议与 S5 同批，权限与隐私政策只动一次 | [~] |
| S5 | 截图识别建日程（=A5） | 前置：探针实测硅基流动 VL 模型 | [~] |

**权限现状（值得守住）**：零敏感权限 —— 无相机/麦克风/定位/通讯录/存储

---

---

## P16 · §57 错误修复批（2026-08-22 拍板即执行）

| | 项 | 状态 |
|---|---|---|
| E0 | 崩溃根因确认：负 padding（premium 标注 -2.dp），已随 v1.9.0 删除，零残留 | [x] |
| E1 | 崩溃 ver 解析文件第一行（不再记成上传时版本） | [~] |
| E2 | 流式空气泡：首个可见增量才插消息；出字后思考行隐藏 | [~] |
| E3 | 周视图窄长块竖排显示标题 | [~] |
| E4 | 未分类配色改方案 A：主题色柔和变体（删散列色） | [~] |
| E5 | 更多页 16→8（订阅·鹿角 / 日历设置收分类 / 数据与维护三合一 / 关于收更新与语言 / 删小鹿行） | [~] |
| E6 | 界面表白式文案全清（品牌句与法律文档除外） | [~] |
| E7 | DeepSeek V4 多模态：探针实测后才排识图 | [ ] 等实测 |

---

---

## P17 · §58 调性统一批（2026-08-22 已执行，待真机验）

| | 项 | 状态 |
|---|---|---|
| T1 | 品牌句 →「做一只更懂你的小鹿」（STORE-COPY 中英） | [x] |
| T2 | PERSONA 方向句（双端） | [~] |
| T3 | 小鹿 tint 补漏（空状态/闹钟页） | [~] |
| T4 | 日抽屉四模式排（日程/任务/表情/日记，弹出即操作） | [~] |
| T5 | AI 页圆角统一 10dp | [~] |
| T6 | 网页圆角 8/9/12/14px → 10px | [~] |
| T7 | 生图模型预留不接入（等方案） | [x] 决定 |

---

---

## P17 · §58 七问排查（2026-08-22 晚）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| X1 | 🔴 流式 fetch 加 25s 超时 | **AI timeout 的根因**：流式路径此前零超时保护 | [x] 已部署 |
| X2 | 流式失败换备用模型重试 | 对齐非流式韧性 | [x] |
| X3 | 扣费移到首字节确认之后 | 此前上游挂起=钱扣了内容没有 | [x] |
| X4 | 失败原因透传 + ai_fail 埋点 | 不再是黑箱 | [x] |
| X5 | 客户端超时 120s→45s | 等两分钟毫无意义 | [x] |
| Y1 | 文案统一「Pro 每天 50 枚」，删 1500 口径 | **1500 是月流量内部口径，被我写进对外语境造成误解** | [x] |
| Y2 | 冷启动拉余额 + 更多页到账提示 + 鹿角页下拉刷新 | 到账正常但用户看不见 | [ ] |
| Y3 | 邀请链接 ?ref= + 一键分享（双端） | 现在只有码要口述 | [ ] |
| Y4 | ⚠️ 送半年 Pro 机制 —— 建议方案 A（注册直接给 180 天） | 等拍板 | [ ] |
| Y5 | P4-2 编辑器三模式切换（Lifebear 新图） | 底部 [📅][✓][☺] | [~] ← App 已做（§71/§75）；网页端仍缺，见 P4-2 |
| Y6 | 提醒自检极简（正常时收成一句话） | | [ ] |

**已核实无误的**：鹿角每日到账 ✅ 邀请奖励双方各+100 ✅ 新手礼+20 ✅ ledger 明细齐全 ✅

---

---

## P18 · §59 鹿角v2 / 生态文档 / 图标（**等拍板**，2026-08-22）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| N1 | ⚠️ 鹿角 v2 拍板：Pro 开卡礼 1000 一次性 + 续订 300 + 日发 30；免费日发 10；注册 100 | 每月复发 1000 会让余额永远用不完 = 额度失去意义 | [ ] |
| N2 | `cap` free 60→200 / pro 300→2000 | **不改则 1000 枚被截断成 300** | [ ] |
| N3 | 注册礼 welcome_bonus 20→100 + 老用户补差 80 | 幂等脚本，先注册的不该拿得少 | [ ] |
| N4 | 🏆 Theme Package v1 标准（11 项 schema + 版本号 + 校验） | 标准没定前做的资产以后都要返工 | [ ] |
| N5 | 🏆 StampInstance：stamp 加 x/y/scale/rotation + 拖拽摆放 | 文档里的金矿：不需 AI，纯工程，手帐感立刻上台阶 | [~] ← §69/§72 已做 x/y+拖拽；scale/rotation 按母档 v1 不做 |
| N6 | 104 枚印章语义标签（semantic/emotion/action）+ AI 按语义推荐 | 零成本让 AI 从录入器变成有眼力见的助手 | [ ] |
| N7 | 抠图方案调研 | **Worker 跑不动去背景** —— 这才是 AI 印章的真卡点，不是生图 | [ ] |
| N8 | 图标方案 B：自绘 8~10 个高频图标 + 随主题 tint | 现状 31 种 Material，气质像系统应用不像手帐 | [ ] |

**与「模型先预留」一致**：N4/N5/N6 不依赖模型选型，可立即推进；N7 及 AI 生成类等模型定案

---

---

## P19 · §60 回到主线·减法（**等拍板**，2026-08-22）

> 检验标准（长期）：**「这让用户记东西更方便了吗？」** 答不上来的不是主线 —— 可以做，但不许占用户视线。

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| R1 | 鹿角减法：12 项机制 → 4 项 | 砍明细页/参考价/里程碑/分级计价；注册+邀请合并；**服务端账本逻辑全保留**，砍的是呈现层 | [~] |
| R2 | Pro 权益表 7 条 → 两句话 | 🚧 规划中的不写进权益表 | [~] |
| R3 | P7-2 定案：撤掉日历顶栏小鹿按钮 | AI 是工具，不该占顶栏；入口留在「更多」与 + 号 | [~] |
| R4 | 日历字号：事件 9→11~12sp / 日期 15→17~18sp 加粗 / 农历弱化 | 视觉重量还给日期与内容 | [~] |
| R5 | 全站扫「要解释的机制」与解释性文案，一并减掉 | | [ ] |

**已拍板记录**：cap free 200 / pro 2000 ✅ ｜ 老用户不补差 ✅ ｜ 落地顺序 Theme Package → 印章可摆放 → 语义标签 ✅ ｜ 图标方案 B ✅

---

---

## P20 · §61 AI 聊天页 UIUX（**等拍板**，2026-08-22）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| Q1 | 🔴 协议补闹钟动作 | **`alarm` 在协议里出现 0 次**：A2 做了真闹钟但 AI 够不着 | [~] |
| Q2 | 🔴 硬规则：没有 action 就不许说"已做好" | 用户问「真的假的」—— 小鹿凭空承诺，这是信任问题不是 UI 问题 | [~] |
| Q3 | 🔴 要么问、要么做，不能又问又做 | 文本问"要不要提醒30分钟"，卡片已设15分钟 | [~] |
| Q4 | 回复精简到一句，细节交给卡片 | 现在同一件事说三遍，与人格设定相反 | [ ] |
| Q5 | 时间格式统一 24 小时制 | 一屏三套：09:00 / 9:00 AM / 8:45 AM | [ ] |
| Q6 | chips 仅空对话时显示 | 深度对话时仍占一行 | [ ] |
| Q7 | 撤销跟随主题色 + 文案缩为「撤销」 | 蓝色图标与青绿主题不搭 | [ ] |
| Q8 | 动作卡片与 AI 气泡层级区分 | 卡片无头像，像两个说话者 | [ ] |
| Q9 | 顶栏垃圾桶+齿轮合并为 ⋯ | 低频操作不占顶栏 | [ ] |

**已生效（本批验证）**：主题色统一 ✅ 小鹿头像随主题 ✅ remember ✅ 动作卡片 ✅ 撤销 ✅

---

---

## P21 · §62 Lifebear 交互骨架对齐（**等拍板**，2026-08-22）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| L1 | 删撤销；动作卡片可点开编辑 | 撤销=「我做错了」低频；点开=查看/改/删三合一 | [~] |
| L2 | 🏆 三模式 tab（日程/待办/表情）贯穿创建编辑页 | = P4-2；四图里三张底部都是这一行 | [~] |
| L3 | 🏆 点日期→底部面板，月视图不消失 + 日记输入区 | 我们现在整页跳转，丢掉日历上下文 | [~] |
| L4 | 🏆 表情所见即所贴（**P7-1 定案：按 Lifebear**） | 现在贴印章要进编辑器填表单，没有贴纸手感 | [~] ← §75 C4 贴纸面板：点选贴纸→点日历连续盖 |
| L5 | 编辑页节奏：行高~76dp / 图标24dp灰线 / 未设置项灰 placeholder | | [ ] |
| L6 | 圆角收敛 12 种 → 4 种（16/10/4/正圆） | 现状 10dp×15、3dp×6、4dp×5… 是随手写的 | [~] |
| L7 | 任务页「详细」折叠低频项 | | [ ] |
| — | §61 Q1/Q2/Q3（AI 提示词） | **暂缓** —— 用户决定：不再加提示词规则，换模型治本 | [~] |

---

---

## P22 · §63 真对比修正（**等拍板**，2026-08-22）

> ⚠️ 记账修正：§62 的 L2/L3 基于错误判断（以为我们没有底部面板/四模式，实际早已有），由本节 M1~M8 替代。

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| M1 | 🔴 日面板去四入口图标，打开即内容 | 我们多一次点击；Lifebear 直接是事件+日记 | [~] |
| M2 | 🔴 空状态「点右上角 ＋」**指错位置** | ＋ 实际在底部中间；且空状态占半屏 | [~] |
| M3 | 🔴 面板底部留导航栏 padding | 日记区被遮（P2-C 老问题复发） | [~] |
| M4 | 🔴 全站 24 小时制（9:00 PM → 21:00） | 编辑页与 AI 卡片都有 | [x] 12h是设置项非bug |
| M5 | tab 选中态 黑底圆角方块 → 浅灰正圆 | 「Lifebear 圆角更好看」最直观一例 | [~] |
| M6 | 面板 emoji → 线性图标（并入 N8） | 两种图标语言混用 | [ ] |
| M7 | 农历并入日期行末（= R4） | | [~] |
| M8 | 「显示详细设置」蓝 → 主题色 | | [~] |
| M9 | 换模型后删格式类提示词（9/10/12），留安全边界（1/2/11） | 12 条规则每条都是踩坑补的 | [~] |

---

---

## P23 · §64 设置页与弹窗规格（**S1 已拍板待执行，其余等拍板**，2026-08-22）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| S1 | 🔴 **提示词删 10 条**，只留「别猜 id」「提问不建条」 | 用户拍板：弱模型时代的补丁，现在只是浪费 token | [~] |
| S2 | 🔴 日历设置加「日程文字大小 大/中/小」默认大 | Lifebear 不替用户决定字号，给三档选 —— 比 §60 R4 猜一个值好 | [~] |
| S3 | maxLines 除数随字号档位（大≈5条，现固定 11dp 挤 8 条） | 依赖 S2 | [~] |
| S4 | 🔴 弹窗圆角 **28dp → 8dp**；单选弹窗去确定/取消 | 「角恰好」的关键：28dp 像玩具，8dp 像工具 | [~] |
| S5 | 圆角档位补弹窗档：16/10/**8**/4/正圆 | 修正 §62 | [~] |
| S6 | 用图示代替文字解释（主题/印章样式/周起始日） | Lifebear 图2 的做法：不是把话说短，是不用话说 | [ ] |
| S7 | 功能入口统一线性图标；分组标题可用 emoji | 纠正 §63 过度判断（Lifebear 设置页分组也用 emoji） | [ ] |
| S8 | 补三个设置项：全天日程样式/定时日程样式/印章旁显示日程名 | | [~] ← 「印章旁显示日程名」§75 M1 已撤（气泡本身删了）；另两项待补 |
| S9 | 📷 日程与日记贴图 | 前置：压缩规格 + **blob 同步通道**（payload 64KB 装不下） | [ ] |

---

---

## P24 · §65 贴图/弹窗/动画/宫格（**等拍板**，2026-08-22 含视频逐帧分析）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| T1 | 📷 贴图：拍照 + 选相册一起做 | 一次权限、一个入口两来源 | [ ] |
| T2 | 🔴 blob 同步通道 | payload 上限 64KB，图片走不了 | [ ] |
| T3 | 压缩：长边1600/WebP/<200KB | 原图 3~5MB 会烧穿额度 | [ ] |
| T4 | Pro 存储：免费 200MB / Pro 5GB | R2 $0.015/GB 且出站免费 → Pro 成本 ≈¥0.55/月（占 ¥12 的 4.6%）；**比鹿角实在** | [ ] |
| T5 | 🔴 LookaDialog 组件（8dp/无确定取消/全站复用） | 现在各写各的 AlertDialog，改一处漏三处 | [~] |
| T6 | 🔴 Motion.kt 动画规范 | 视频量得：按压120 / 进入280 / 退出180 / 分层淡入；**我们现在比 Lifebear 快30%，少了从容** | [~] |
| T7 | 更多页 → 3 列宫格 | Lifebear 一屏 9 个不用滚 | [~] |
| T8 | 空状态箭头指向真实操作位 | 修 §63 M2 硬错误 | [~] |
| T9 | 加载弹窗统一（小方框+小鹿+一句话） | 替换转圈 spinner | [~] ← §70 E 批 DeerLoading 已落地 |
| T10 | 自创主题色板 18 → 48 色 | | [~] |
| T11 | 日期跳转加「±1年」快捷 | | [~] |
| T12 | 图示代替文字（四处） | 「不用话说」全局原则 | [ ] |

**全局原则（用户定）**：能用图示说明的不写解释文字；能用默认值决定的不问用户。

---

---

## P25 · §67 事故修复与架构研究（**等拍板**，2026-08-22）

| | 项 | 关键点 | 状态 |
|---|---|---|---|
| X1 | 🔴 **AI 小鹿入口恢复** | 事故：§57E5 删更多页行 + §60R3 删顶栏，两次都假设"另一处还有" | [~] |
| X2 | 🔴 立规矩：删入口前 grep 全部可达路径，确认剩 ≥1 条 | 根因是流程缺陷不是手滑 | [ ] |
| X3 | E3 退回 maxLines=2（窄块竖排"拉长"是它的副作用） | K1 已让窄块大幅减少，E3 可退 | [~] |
| X4 | 后台 8 个指标显示 undefined → 补统计或显示 — | 用户截图实锤 | [x] |
| X5 | ⚠️ 未分类配色：主题色变体 vs 中性灰，二选一 | 现状是你拍板的方案A | [x] 拍板中性灰(BEAR实锤) |
| X6 | 更多页按语义 6 组重构（11 项，含 AI 入口） | 上一版是"按重要性排"，Lifebear 是"按语义分组" | [~] |
| X7 | 反馈与建议系统 | 文档建议 V1 必做，我同意；不带日记/笔记正文 | [ ] |
| X8 | user_identities 表 | 8 个用户时做，比 8000 个时便宜一万倍 | [ ] |
| X9 | 长期记忆独立实体（现挂 settings 里） | | [ ] |
| X10 | Model Registry（env → 表） | | [ ] |
| X11 | ⚠️ GitHub remote 切 looka（**当前指向 foyue01，44 commit 未推**） | 需先确认 looka 仓库是否为空 | [x] 已推44commits |
| X12 | release.sh：push 成功才部署 | | [x] |

**建议不接**：Clerk（Android 支持弱/大陆风险/账号跨产品共享/8用户不值） · Supabase PG（D1 够用，到 1GB 再评估） · PostHog · Sentry
**采纳其思想不采纳其实现**：identity 分层（X8）· AI Gateway（已部分具备）· 长期记忆独立（X9）

---

---

## P26 · §69 系统性升级（梯队Ⅰ-Ⅱ 已执行 v1.11.0，2026-08-23）

| | 项 | 状态 |
|---|---|---|
| Y0 | AI 内部 ID 显示层清洗（双端 4 处） | [~] |
| Y1 | Sticker Canvas：Room v6 posX/posY + 同步双向 | [~] |
| Y2 | 视觉 0.42×cellW 标准化 + 虚线圈 1.02×cellW | [~] |
| Y3 | 长按拖动 + 落点换日（跨周） | [~] |
| Y4 | 印章点击菜单（建日程/删除） | [~] |
| Y5 | 网页印章画布同构渲染 | [~] |
| Y6 | Today 悬浮返回 | [~] |
| Y7 | 全天区长按建全天日程 | [~] |
| — | CAL-070 面板 / 时间格长按 | [x] 查证已符合规格 |
| — | 缩放/旋转 | 按文档 v1 不做（Lifebear 实机无此能力） |

**下一梯队（顺序已定）**：Ⅲ 创建引擎（Event 默认值页 + 模板系统）→ Ⅳ 情绪层（空状态统一/图示化/加载品牌化）
→ Ⅴ 服务生态（反馈 X7 / identities X8 / 记忆实体 X9 / CAL-072 显隐层）；并行 N4 Theme Package · N6 印章语义标签

---

## P27 · §70 梯队Ⅲ+Ⅳ + 周视图可读性 + 缓存补漏（v1.12.0，2026-08-23）

| | 项 | 状态 |
|---|---|---|
| W1 | 周视图泳道上限 2，溢出折叠 "+N" 徽章 | [~] |
| W2 | 点 "+N" 切到当天日视图 | [~] |
| W3 | 块宽 <20dp 不画文字 | [~] |
| X1 | 网页流式气泡补 ID 清洗 | [~] |
| X2 | 静态资源 ?v= 版本参数 + SW VER + release.sh 自动盖章 | [~] |
| T1 | 模板管理页（日历设置入口/列表/删除） | [~] |
| T2 | 独立模板编辑页（标题/全天+时间/分类/提醒/地点/备注，无日期无重复） | [~] |
| T3 | 创建流模板面板「管理模板」互通 | [~] |
| T4 | 网页模板 chips + 存为模板（localStorage） | [~] |
| E1 | DeerLoading 组件 + 日记润色等待态 | [~] |
| E2 | 设置弹窗图示化：字号所见即所得 / 周开始日预览 / 休日星期名变红删解释句 | [~] |
| — | CAL-061 默认值 | [x] 查证已达标，未重复做 |
| — | 空状态统一 | [x] 查证 EmptyDeer 已覆盖 10 处 |

---

## P28 · §71 周视图形态 + 创建面板 + AI 入口 + 印章手感（v1.13.0，2026-08-23）

| | 项 | 状态 |
|---|---|---|
| A | AI 顶栏小鹿入口 ×3（日历/待办/笔记，推翻 §60 R3 依用户拍板） | [~] |
| W4 | 周视图压缩形态（时间栏 24dp/纯数字小时/全天区收紧） | [~] |
| N1 | 创建面板：月视图 ＋ → 底部停靠（日程快建/任务快建/表情连续盖章） | [~] |
| S1 | 印章视觉 0.42→0.50×cellW（用户对比 BEAR 拍板，偏离文档范围记录在案） | [~] |
| S2 | 拖动激活震动反馈 | [~] |
| S3 | 网页印章 42%→50% 同步 | [~] |

---

## P29 · §72 Sticker 模块工程复刻（母档 v1.0，v1.14.0，2026-08-23）

| | 项 | 依据 | 状态 |
|---|---|---|---|
| G1 | 🔴 去长按前置，改 touchSlop 直接拖（"拖曳不灵敏"根因） | §4.2 | [~] |
| G2 | 拖动时锁 Calendar 滚动（Pointer ownership） | §8 / AC-010 | [~] |
| G3 | DragRing 1.02→1.08×Wd | §2.2 | [~] |
| G4 | Visual 0.50→0.47×Wd（规格上限）；hit 0.95×Wd（视觉≠交互） | §2.2 / §3 | [~] |
| G5 | Picker Preview 0.60×Wd 自适应（双尺度） | §2.2 / §11.1 | [~] |
| P1 | Popover 换轻量锚定气泡：白底细边·两等分·中央分隔·caret·无遮罩·靠边 clamp | §5.1 | [~] |
| P2 | 两态：未绑定「登记日程/删除」· 已绑定「编辑/删除」 | §5 / §9.1 | [~] |
| P3 | 🔴 已绑定的「编辑」进这条日程（旧实现总是新建一条 —— bug） | §5 | [~] |
| P4 | Decorative→Bound 回绑（保存后写 eventUid；取消不绑） | §5.2 / §9 | [~] |
| B1 | 绑定后印章上方灰色标题气泡 | §6 / AC-006 | [~] |
| B2 | 拖动时气泡与印章作为复合对象同移 | §6 / AC-008 | [~] |
| B3 | 全局开关「显示印章上的日程名」+ 双端同步 | §6.1 / CAL-060 | [~] |
| B4 | 月格去重：有气泡就不再重复出普通事件块 | §6.1 | [~] |
| V1 | Picker 打开只让出 viewport 底部，不压缩格子 | §11 / AC-013 | [~] |
| D1 | 从 Picker 拖拽创建（Ghost + DateCell 命中 + u/v 落库） | §4 / AC-001~003 | [~] |
| D2 | New Drop 自动弹语义确认；Reposition 松手不弹 | §4.1 / AC-004/009 | [~] |
| W1 | 网页同构：0.47 尺寸 + 标题气泡 + 设置跟随 | 双端规矩 | [~] |
| — | 无惯性/无 pickup 放大/无旋转/无 spring | §10 / AC-014 | [x] 查证本就符合 |
| — | hostDate + cell-local u/v 坐标模型 | §7 / AC-012 | [x] 查证本就符合 |
| — | Sticker/Ring/Bubble/Popover 分层渲染 | §1.1 | [x] 查证本就符合 |
| — | 震动反馈 | §10 Haptic=V 级 | 已移除（改直拖后跟手本身即反馈） |

**两处主动偏离（记录在案）**：① 点选贴纸→点日期"连续盖章"不弹菜单（弹菜单只用于拖拽创建，
否则打断连续贴）；② hit 取 0.95×Wd 而非满圈 1.08×Wd，避免抢邻格命中。

---

## 技术债（不紧急，但记着）

| | 项 | 出处 |
|---|---|---|
| TD-1 | 网站无 CSP；将来加 CSP 时爱发电的内联 script 会被拦 | §33 |
| TD-2 | `users.created_at` 是 TEXT，其余时间戳是 INTEGER 毫秒 —— 统计要特判 | §43 |
| TD-3 | `/api/admin/*` 在鉴权段之后，必须先登录普通账号才够得着（新后台不要沿用） | §43 |
| TD-4 | CSS 新增更长选择器 = 悄悄提高特异度，必须回头搜被覆盖的旧规则（W3/W4 就是这么来的） | §40 |

---

## 建议执行顺序

```
① P0-1（改 Webhook URL）           ← 你点一下，支付线立刻从「次日」变「秒级」
② P1 全部（埋点）                   ← 唯一有时效性
③ P2-A（订阅闭环）                  ← 正在漏钱
④ P2-B + P2-C（月历 + 网页 bug）    ← 每次滚动都在闪 / 底栏点不到
⑤ P2-D + P2-E（AI 提醒 + 可观测）
⑥ P6（上线前必改）                  ← 发版前必扫
⑦ P3（管理后台三期）
⑧ P4 + P5（对齐与机制）
```

---

## v1.8.0 实测待办（2026-08-22 发版，代码已上线，**这些必须你在真机上验**）

> 打勾规则：勾了代表**实测通过**，不是"代码写完了"。下面几条我无法代你验证。

| | 项 | 怎么验 | 状态 |
|---|---|---|---|
| V-1 | ⏰ **闹钟静音也响** | 手机调静音 → 更多 → 提醒自检 → 「⏰ 测试闹钟」→ 锁屏等 10 秒 | [ ] |
| V-2 | 锁屏全屏弹出 | 同上，看是否直接亮屏弹全屏页 | [ ] |
| V-3 | **持续响**（不是一声） | 不按停止，确认铃声不停（最长 5 分钟自动停） | [ ] |
| V-4 | 停止 / 再响5分钟有效 | 分别点两个按钮 | [ ] |
| V-5 | 普通提醒仍只响一声可划走 | 建一条不开「当成闹钟」的提醒 | [ ] |
| V-7 | AI 删除会先弹确认卡 | 说「删掉今天所有日程」→ 应出现勾选卡 | [ ] |
| V-8 | 撤销有效 | 执行后点「↩️ 撤销刚才的修改」 | [ ] |
| V-9 | 清理重复日程 | 更多 → 清理重复日程（修历史重复数据） | [ ] |
| V-10 | 主题上云 | App 换主题 → 网页刷新 → 应同步变色 | [ ] |
| V-11 | 小鹿图标随主题变色 | 换主题看徽章底色 | [ ] |
| V-12 | FREE 10 次/天生效 | 免费账号连聊 11 次，第 11 次应被温和拦下 | [ ] |
| V-13 | ⚠️ **「更聪明」实测** | 需登录态，我无法代验：切到「更聪明」问一句，看是否正常回答且标注 DeepSeek | [ ] |
| F-6 | Ko-fi 后台贴英文文案 v2 | 文案已备好；月费/年费支付页直链已登记进 `STORE-COPY.md` | [ ] |
| F-5b | 爱发电后台贴中文文案 v2 | 同上，**两处都要改** | [ ] |

---

## v1.9.0 执行总结（2026-08-22，「全部执行」批次的对照说明）

**已执行并上线（[~] = 代码完成待真机实测）**：P12 的 D1/D2/D4、W1/W2、U1/U3/U4/U5/U6、M0~M6、T1/T2/T3；
P13 的 C1~C4、K1/K2/K3、G0~G7；P14 的 P1~P6、P8。

**未执行的项与原因（按用户规矩：没做的必须说明）**：

| 项 | 原因 |
|---|---|
| W3 空白时段折叠 / W4 双指缩放 / W5 三日视图 | §52 已标为低优先；W1+K1+K2 已解决截图暴露的可读性问题，这三项属于进一步增强，留待周视图实测反馈后再做 |
| U2 圆角统一 10dp | 气泡 14dp 的差异属于低风险视觉项，本批改动已极大，刻意少动一处以控回归面；下批处理 |
| U7 App 录音中麦克风脉动 | App 走系统语音识别界面（系统接管录音态），App 内无录音中状态可做动画；网页端已有（⏹ 红色态）—— **此项在 App 侧不成立** |
| D3 端上 patterns（任务推迟次数等） | 需要新增行为埋点（本地推迟计数），涉及数据模型；本批已有 Room 迁移风险，不再叠加。现有统计（本月日记数/未完成任务数）已注入上下文 |
| M6 premium 代码「注释保留」 | 改为 **git 历史指针**（v1.8.2 tag 可整段恢复）——80 行注释代码比一行指针更碍事，语义相同 |
| G5 余额不足引导文案 | 已并入服务端 429 文案实现（「今天的鹿角用完啦…」），不需要单独客户端实现 —— **已覆盖，非未做** |
| P7 鹿角开卖 | §55 定案：定价 ≠ 开卖，7 个用户无规模，刻意推后 |
| C5 premium 重试改 1×20s | premium 分支已随 M1~M4 整体下线，**此项已无意义** |

**新增真机实测项（追加到 v1.8.0 实测清单之后）**：

| | 项 | 怎么验 | 状态 |
|---|---|---|---|
| V-14 | 鹿角每日到账 | 打开 App 进小鹿页 → 应见「+10 🦌 今天的鹿角到账啦」 | [ ] |
| V-15 | 聊天扣 1 枚 | 我的鹿角 → 明细应出现「和小鹿聊天 -1」 | [ ] |
| V-16 | 流式回复 | 发消息 → 文字应逐段出现而不是转圈后整段蹦出 | [ ] |
| V-17 | 流式不露 JSON | 说「明天下午3点开会」→ 过程中不应看到 ``` 或 {"actions" | [ ] |
| V-18 | remember | 说「记住我不爱早上开会」→ 出现「记住啦」→ 订阅页能看到并能忘掉 | [ ] |
| V-19 | 周视图不再压碎 | 建一根 9:00-14:30 长日程 + 几个短日程 → 短日程应占满剩余宽度 | [ ] |
| V-20 | 周视图打开位置 | 下午打开周视图 → 应停在当前时段附近而不是 7:00 | [ ] |
| V-21 | AI 建日程有颜色 | 让小鹿建日程 → 时间轴上不应是灰色 | [ ] |
| V-22 | 邀请奖励 | 用邀请码注册新号 → 双方各 +100（明细可见） | [ ] |
| V-23 | 崩溃补传 | 若再遇崩溃：重启 App → foyue.org/admin → 崩溃 tab 应出现记录 | [ ] |

## P30 · §73 创建面板等高 + 任务标记统一（v1.14.1，2026-08-23）

| | 项 | 状态 |
|---|---|---|
| 1 | 创建面板日程/任务/表情三态等高 300dp（切 tab 不跳变） | [~] |
| 2 | 月格任务条 ○未完成/✓完成（原恒为✓） | [~] |
| 3 | 日详情抽屉任务 圆圈/圆圈打勾（原未完成是日历图标） | [~] |
| 4 | 网页月格 ○/✓ + 日面板圆钮（弃 ☐/☑ 方框） | [~] |

## P31 · §74 Sticker→Event 数据链路 8 个 P0（v1.15.0，2026-08-23）

| | 项 | 状态 |
|---|---|---|
| P0-2 | 命中改 layoutInfo 真实几何（12/17≠12/10 错一周根治） | [~] |
| P0-1 | Ghost 持留到实例落库（放下不再"消失"） | [~] |
| P0-3 | Popover 锚点=落点=实例中心（随 P0-2 对齐） | [~] |
| P0-5/6 | 绑定关系响应式（collected series）+ 去 remember 陷阱 + 网页月格补去重（双重 Event 根治） | [~] |
| P0-7 | 原子移动：印章+绑定日程同 delta（残影根治；详情页"已改"是假象已证） | [~] |
| P0-4 | 登记日程表单显示贴纸缩略图（Context continuity） | [~] |
| P0-8 | 绑定保存后 Composer 收束 | [~] |
| P0-10 | NavHost 全局 Horizontal Push（无 crossfade 叠影） | [~] |
| B | 任务完成去删除线 ×6（双端） | [~] |

## P32 · §75 Composer 三面形态 + Sticker 模型修正（v1.16.0，2026-08-23）

| | 项 | 状态 |
|---|---|---|
| M1 | 删标题气泡 + 撤「显示印章上的日程名」设置（月格只有贴纸，图42/45） | [~] |
| M2 | 日详情事件行贴纸缩略图（双端） | [~] |
| M3 | Popover 间隙 0.09→0.17×Wd | [~] |
| M4 | 贴纸 0.47→0.42×Wd（双端） | [~] |
| C1 | 面板收高 ≈238dp（网格 188→120 · 删 300dp 等高外框 · 去提示文案） | [~] |
| C2 | 面板打开隐藏全局底栏 | [~] |
| C3 | 🔴 格子不再被面板压扁（父层 padding → LazyColumn contentPadding） | [~] |
| C4 | Composer 三面：日程/任务全屏（底部保留切换器）· 贴纸=月历+面板 · ＋记住上次的面 | [~] |
| T1 | 任务面 ○ 可勾 + ☆ 星标 + 清单行 + 预填选中日 | [~] |
| D1 | 拖动代理：原位半透明留守，副本跟手，落位后原位消失 | [~] |
| — | 相机 📷 链路 → §76 单独批 | [ ] |

## P33 · §76 Popover 坐标系根治 + 绑定贴纸编辑/删除闭环（v1.16.1，2026-08-23）

| | 项 | 状态 |
|---|---|---|
| F1 | 🔴 Popover/Ghost 坐标系换算（窗口坐标 → 容器内坐标）—— 遮挡贴纸的真根因 | [~] |
| F2 | 绑定贴纸「编辑」→ 全屏编辑页；编辑态也显示贴纸缩略图（任何入口一致） | [~] |
| F3 | 删除确认框 + 复合删除（贴纸与绑定日程原子同删，双端）—— 根治孤儿日程 | [~] |

## P34 · §79 L001 自审三修（批 E'，2026-08-24）

§78 的「四修」经回查更正为三修：多选那条降级为一行、深页返回转真机复验、
Task 原生详情页挪进批 B。详见 FEATURE-PLAN §79。

| | 项 | 状态 |
|---|---|---|
| E1 | 🔴 提醒自检 ON_RESUME 重查（`tick` 从未被 ++，页面状态进来就冻结，而文案写着「改完回到本页会自动刷新」= 骗人） | [~] |
| E2 | 休日星期多选 `onDismissRequest` 回滚（点遮罩外/返回键不再残留脏勾选） | [~] |
| E3 | 🔴 AI quick chip 改预填不自动发送（误触即扣 1 鹿角；本周总结用「填人话+记住动作」处理）**双端** | [~] |
| V1 | ⚠️ 真机复验：更多页滚到底 → 进日历设置 → 返回，是否回页顶（代码上 NavHost 应已保留） | [ ] |

**改动位置**：`ExtraScreens.kt:521` SelfCheckScreen 加 ON_RESUME 观察者 ·
`CalendarSettingsScreen.kt:301` onDismissRequest 回滚 ·
`AiChatScreen.kt:85` 新增 `chipAction` + 统一 `send()`，chip 改为填输入框 ·
`server/public/app.js:1864` 网页 chip 同步改为填 `#chatText` 并聚焦。
Kotlin 编译通过；网页端要等下次 `release.sh` 才会刷 `?v=`（脚本按 versionCode 自动改，未手动动）。

**§15 两端同步核对**：E3 双端已同步；E1 网页无提醒自检页、E2 网页设置未暴露休日多选（只同步值，
`app.js:155/235`）—— 两条属 §15 ③「平台能力所限，允许单端」，不欠债。

**已结案（无需改）**：AC LOOKA-102 主题提交语义 · LOOKA-103 AI 权限域（两域独立开关，
默认值与文案一致，`LookaViewModel.kt:745` / `DiaryEditScreen.kt:161` 真生效）· LOOKA-106 关于弹窗。

**挂账 P1（§79 四）**：AI 入口命名易混 · 关于 Dialog 升全页的触发条件 · 次级灰字对比度审查。

## P35 · §80 AI 上游债清理（2026-08-24）

`premium_fail` 自 §53 下线 premium 分支后再无人发送，四处监控却都在查它 ——
Qwen 真挂了后台不响、看板恒显 0。真实埋点是 `ai_fail`。详见 FEATURE-PLAN §80。

| | 项 | 状态 |
|---|---|---|
| G1 | 🔴 非流式 502 补发 `ai_fail`（此前只有流式路径发，非流式失败静默） | [~] |
| G2 | 🔴 每日自检告警改查 `ai_fail`（这条告警从 §53 起就是哑的） | [~] |
| G3 | 健康接口 `premium_fail_24h` → `ai_fail_24h` | [~] |
| G4 | 后台 `foyue-admin` worker + 前端同步改（含 KIND_CN 加 `ai_fail`） | [~] |
| G5 | §0 加更正框：线上是 Qwen，GPT 实测表只证明「Worker 出口能连通」 | [~] |
| G6 | ✅ 已拍板「删除」：`aiProviders()/aiRequest()` 整段 54 行移除，零残留；`pickText` 增强保留（§82 二1） | [~] |

**验证**：两个 worker `node --check` 通过。G1~G4 要等部署后看后台「AI 上游失败」是否开始有数
（现在应为 0 或 —，不再是假的"健康"）。

## P36 · §81 上架前置 + 无障碍量化（2026-08-24）

| | 项 | 状态 |
|---|---|---|
| H1 | ⏸ 备案/域名 —— **用户 2026-08-24 定：先不讨论**，条目保留不推进 | [ ] |
| H2 | 🔴 AI 显式标识**已加**（聊天页一行，双端）。⚠️ 更正：真 AI 生成只有小鹿对话一处 —— 主题是本地取色、表情包未上线（§82 一）。隐式标识与上架材料留到表情包上线时办 | [~] |
| H3 | ✅ 已拍板：灰字 `#8A8F8E` → `#727776`（白底 4.55:1 达标）。App `Theme.kt:18` + 网页 `style.css` 9 处，**两端同步** | [~] |
| H4 | ⏸ 休日红 `#E0504A` 白底 3.88:1，作纯文字色不达标（作色块/标记无妨）—— 未动，动它会改休日观感 | [ ] |

**量化口径**：WCAG 2.1 相对亮度公式，正文 AA = 4.5:1、大字（≥18pt 或 ≥14pt 粗体）= 3.0:1。
主文字 `Ink #1B1B1F` 17.17:1，很安全。

## P37 · §77 批 A（2026-08-24 补建清单）

⚠️ 这批在 §77（8-23）就定了，但**从未进过执行清单** —— 计划立了没落到唯一勾选处，
9 项悬空一整天。本节补建。N3（笔记清单结构）不属批 A，归批 C。

| | 项 | 状态 |
|---|---|---|
| N9 | 🔴 **真 bug 已修**：`Nav.kt:241` onPlus 改为按 tab 分流（待办=建任务 · 笔记=建笔记/日记 · 日历+更多=维持 Composer 三面）。`notesSeg` 提到 VM 供 ＋ 判断子 tab。**单端 bug** —— 网页 `app.js:1579` 本来就分流了（注释写「与 App 一致」，其实 App 才是错的那端） | [~] |
| N8 | 小鹿挪进搜索行右端 + 撤掉笔记页顶栏重复的 ＋（双端）。网页同步撤掉 `#btnAddNote`/`#btnAddDiary`，中央 ＋ 改为直调函数而非 click 已删按钮 | [~] |
| N6 | 笔记页搜索框置顶、tab 移到其下（对齐图5 层序）；搜索真生效：笔记搜标题+正文、日记搜正文，空结果有专属空态。双端 | [~] |
| N7 | placeholder 写清可搜内容：「搜索笔记名、正文」/「搜索日记正文」。双端 + i18n 英译已补 | [~] |
| N5 | 待办页删「待办」标题行，搜索条上提为页首；小鹿沿用 N8 手法收进搜索行右端。**单端**：网页待办本来就没标题行 | [~] |
| N4 | 顺序改为 搜索 → 星标/未来7天 → 清单 → 已完成。**单端**：网页 chip 布局（全部/⭐/未来7天/清单）本来就是这个序 | [~] |
| N2 | 体检出报告 + 只做一条减法：撤掉「数据」组的「搜索」行（搜索是内容操作不是产品设置，归类本就错）。删前查证可达路径 3→2 条（日历页/待办页页首仍在）。**有意单端**：网页 `mSearch` 是全局搜索唯一入口，删了功能就没了 —— 见下方偏离记录 | [~] |
| N1 | 心情从顶部 5 个 42dp 大圆圈挪到正文下方一行 30dp 小图标，未选中压暗到 0.4 —— 不再开口就问「你今天心情如何」。双端（网页日记弹窗同步调序） | [~] |

**双端**：N9/N8/N6/N7 网页端已同步；N5/N4/N2 见各行的单端说明。

**🔻 有意偏离记录（N2）**：更多页「搜索」行只在 App 删，网页保留。
理由是可达路径不同 —— App 侧删后仍有日历页（`CalendarScreen.kt:1362`）与待办页页首
（`TodoScreen.kt:89`）两条路；网页侧 `#mSearch` 是全局搜索的**唯一**入口，删掉等于删功能。
这条偏离是刻意的，不是漏做；网页要跟进的话得先给日历/待办页补搜索入口（另开条目）。

## P38 · §85 批 B（父/子编辑器骨架 + Task 详情，2026-08-24）

| | 项 | 状态 |
|---|---|---|
| B4 | `rowClick` 落地：NavRow（全站导航行）/HubRow/待办清单行/任务行/笔记·日记行/编辑器分类行/搜索条；网页 `.more-row/.note-item/.diary-item/.todo-item/.lchip/.chip:active` | [~] |
| B1 | 分类全页 select-and-return（`catPick`）上线；`CategoryPickerSheet` **保留**给模板编辑器（其 draft 是屏幕局部的，见挂账） | [~] |
| B2 | 提醒两层全页上线：`reminders`（启停/删除/闹钟式，＋进创建）+ `reminderNew`（X 取消 · 深色 Save · 快捷 radio · 全天另有提醒时刻）。`ReminderSheet` 保留给模板编辑器 | [~] |
| B3 | ✅ 已存在（§77 未查现状就列了）：tab+摘要+间隔+结束日全有。偏离记录：Stepper 代数字键盘（C 级，防非法值） | [x] |
| B5 | `TaskDetailScreen` 上线：完成圆/星标直切无确认 · More=DropdownMenu 锚定无遮罩（复制/删除）· Copy=`TaskEditDialog(isNew)` 预填新对象 id=0，保存才 addTask · Delete 二次确认。4 处行点击改进详情（第 5 处是已完成页，行为不变）；3 处死状态清理 | [~] |

**挂账（§15 ②）**：网页 B1/B2/B5 仍是弹窗表单，commit 语义一致、形态差异下批跟。
**挂账（批内）**：模板编辑器（TemplateScreens）用屏幕局部 draft，接不上读 `vm.draft` 的全页选择器，
本批继续用旧 Sheet —— 要跟上得先把模板 draft 提升到 VM，另开条目。

## P39 · §86 批 C 笔记清单合同（V014 B 级，2026-08-24）

| | 项 | 状态 |
|---|---|---|
| C1 | `NoteList` 实体 + `note.listUid` + **Room v6→v7 迁移**（注册在 `LookaApp.kt`）+ `NoteListDao` + 惰性默认清单 `nlist-default`（不可删）。删清单不删笔记：`reassignList` 迁回默认。**迁移已逐列比对 Room 生成的 DDL**（列名/类型/NOT NULL/索引名 `index_note_list_uid`、`index_note_listUid` 全对上） | [~] |
| C6 | `notelist` kind 三端就位（SyncEngine push/pull/ack + worker `KINDS` + 网页 `KINDS`）；note payload 带 `listUid`；拉取排序里 notelist 与 tasklist 同级先落地（笔记引用它） | [~] |
| C2 | 清单 chips 双端：横滑、选中高亮、带条数、末尾 ＋ 建清单；先按清单收窄再按搜索过滤 | [~] |
| C3 | 编辑页清单行 → `NoteListChangeDialog` → `NoteListCreateDialog`：**suspend/replace**（开创建层时收起选择层，取消再放回）· 创建层是唯一 IME owner（FocusRequester 自动聚焦）· 建好自动选中 · **空名/重名不关弹窗**就地报错 · 全程不碰 title/body draft。网页用 select + prompt（形态差异见挂账） | [~] |
| C5 | 空态分三态：搜索无果 / 「某清单」里还没有笔记 / 全局还没有笔记。双端 | [~] |
| C7 | 网页：`noteChips` + `curNoteList` 收窄 + 新建即选中 + 编辑弹窗清单 select + `ensureNoteListDefault`（同 uid `nlist-default`，跨端天然合并） | [~] |
| C4 | ⚠️ **主动偏离**：不引入 DiscardDialog，保留「返回即静默保存」——理由见 §86 二 | [x] |

**🔴 真机必验（迁移）**：这批动了 Room schema（v6→v7）。**装旧版 → 写几条笔记 → 覆盖安装新版**，
确认笔记一条不少且都在「我的笔记」里。迁移写错的后果是用户要卸载重装，编译通过不代表迁移对。
**挂账**：网页建清单用的是 `prompt()`，没有配色选择；App 侧有 48 色板。下批统一。

## P40 · §87 批 D Overlay 合同（2026-08-24）

| | 项 | 状态 |
|---|---|---|
| D1 | 逾期询问加静默窗守卫（`Nav.kt`）：仅 `tab==0` 且创建面板未开时才弹；不满足条件当天不再补弹。**单端**：网页无此弹层 | [~] |
| D2 | `keyboardAwareOpen`（`Ui.kt`）+ EventForm 内 `kbAware`：开日期/时间层前先 `clearFocus(force)` + 收键盘。已接 4 个触发点（起止日 × 日期/时间）。**单端**：网页用原生 `<input type="date">`，无此问题 | [~] |
| D3 | `LookaDatePicker` 加 `fieldLabel`：顶部显示「开始日期 / 结束日期 + 当前值」，弹层盖住父页时仍知道在改哪个字段。不传 = 原行为不变（其余 6 处调用点零改动） | [~] |
| D3b | 结束日改内联展开：抽出 `LookaMonthPanel` 供 Dialog 与内联两个变体共用（此前月格内联在 Dialog 的 text 槽里，做内联只能复制一份）。内联变体点一下即回填 + 「未设置 / 收起」两个动作 | [~] |
| — | ⚠️ **不做且记录在案**：Overlay Manager 队列/频控引擎（AI 主动建议与运营弹窗都不存在，无消费者）；`toast`→Snackbar（现状已非阻断，等做「撤销」时一并换）。理由见 §87 二、三 | [x] |

## v1.17.0 真机验证清单（发布前必须走完，2026-08-24）

批 A–D 共 26 项全是 `[~]`。**这份清单走完才允许跑 `scripts/release.sh`。**
顺序有讲究：第 0 项不过，后面都不用测了。

### 0 · 🔴 迁移升级路径（唯一一个「验错要用户卸载重装」的）

| | 步骤 | 期望 |
|---|---|---|
| M1 | 先装**旧版 v1.16.1**（R2 上的 looka-latest.apk 现在还是它），写 3 条笔记、1 条日记、2 个任务 | 正常保存 |
| M2 | **覆盖安装** v1.17.0（不要卸载！卸载重装等于没测迁移） | 能装上、能打开 |
| M3 | 看笔记页 | 3 条笔记一条不少，且都在「我的笔记」清单里 |
| M4 | 看日记、待办、日程 | 数据都在，没有闪退 |

M2 若崩溃或 M3 少数据 → **立即停止发布**，回来查 `MIGRATION_6_7`。

### 1 · 批 A（§77）

- 笔记页按底部中央 ＋ → 出来的是**新建笔记**（不是日程）；切日记 tab 再按 ＋ → 今天的日记
- 待办页按 ＋ → 直接建任务
- 待办页顺序：搜索条在最上 → 星标/未来7天 → 清单 → 已完成
- 笔记/日记搜索框实时过滤，清空能还原
- 日记编辑页顶部**没有**表情排，心情在正文下方一行小图标
- 更多页「数据」组只剩「备份与维护」，但日历页和待办页搜索入口都还在

### 2 · 批 B（§85）

- 日程编辑 → 点分类 → **整页**选择器 → 点一个颜色**自动返回**，父页色点立即变
- 点提醒 → 列表页 → ＋ → 创建页选「30分钟前」→ Save → 回列表；父页摘要更新
- 提醒创建页按 **X 退出 → 什么都没留下**
- 待办点任务行 → 进**详情页**（不再直接弹编辑框）；完成圆、星标点一下就变
- 详情页 More → 复制 → 改名保存 → **原任务没变**，多出一条新的
- 各处行按下去有浅灰按压反馈

### 3 · 批 C（§86）

- 笔记页有清单 chips；建一个新清单 → **自动切到它**
- 编辑笔记时改清单 → 正文没丢
- 新建清单时输空名或重名 → **弹窗不关**，就地报错
- 网页端登录同一账号 → 清单同步过去了

### 4 · 批 D（§87）

- 有逾期任务时进 App：弹窗只在**日历页**出现（在待办页启动不该弹）
- 日程编辑器：标题输入着（键盘起）直接点日期 → 键盘先收干净再出月历，**不卡空窗**
- 月历顶部标着「开始日期」还是「结束日期」
- 重复页点结束日 → **就地展开**月历，不是弹窗

### 5 · 发布后（release.sh 跑完）

- `curl -s https://looka.foyue.org/version.json` 显示 1.17.0 / 34
- 网页硬刷新，确认 `app.js?v=34` 生效（笔记 chips 出现）

## P41 · §88 v1.3 剩余 AC 收口（2026-08-24，发布前）

| | 项 | 状态 |
|---|---|---|
| S1 | 🔴 模板编辑器 X / 返回键加 dirty 确认（AC SET-104）—— 此前 `TemplateScreens.kt:145` 直接 `popBackStack`，输一半误触 X 全丢。Event 编辑器早有这道守卫，模板漏了 | [~] |
| S2 | 休日多选改真 draft（AC SET-102 完整版）：Dialog 内用局部 `tmpMask`，确定才写回页面态与 Prefs；取消/点外/返回键零副作用。§79 只补了取消回滚，勾选仍在实时改背景摘要 | [~] |
| S3 | ✅ 查证后**无需改**：`saveNote`（`LookaViewModel.kt:629`）本就守卫「标题正文都空则不落库」，空白新笔记不会产生空记录 | [x] |

**已闭合不再动**（§88 四逐条对过代码）：SET-101 / SET-103 Capability Gate（`customGate` 只在直接点击后置位）/ TSK-101~104 / NOTE-101~103·105 / OVL-101·102；OVL-103 无 AI 主动层 N/A。

**有意偏离**：NOTE-104 笔记返回是「静默保存」而非 discard 合同 —— 保留现有行为。
自动保存对写字的人比「你要丢弃吗」更友好，笔记也没有「未完成状态」概念；让利于用户优先。

⚠️ **12:21 那个 APK 不含 S1/S2，发布前必须重新出包。**

## P42 · §89 Popover 对照复刻 + 贴纸视觉尺寸（2026-08-24）

证据：`参考组件图标/0824/微信图片_20260824145651_69_13.jpg` [B]

| | 项 | 状态 |
|---|---|---|
| U1 | 🔴 贴纸画布 0.42 → **0.50 × Wd**（双端）。根因不是参数小：素材 256 画布装 218 主体（安全区，实测三套 84.8–85.9%），0.42 施于画布 → 视觉仅 0.358×Wd，比实机 0.43 小 17%。母档说的是 **visual**，我们套错了对象 | [~] |
| U2 | Popover 高 0.62 → **0.48 × Wd**（实机 78/162.3） | [~] |
| U3 | 描边 `#DCDFDC` 0.8dp → **Ink 1.2dp**（含分隔线与 caret 描边）—— 实机靠深色描边成形，浅灰几乎看不见 | [~] |
| U4 | 「删除」字色 红 → **黑**（实机「削除」与「编集」同色；危险语义由确认框承担） | [~] |
| U5 | 🔴 **caret 跟随锚点**：原 `padding(start = w)` 写死在左侧 12%，贴纸居中时指偏、靠边 clamp 后指向无关位置 | [~] |

**单端说明**：U2–U5 网页无贴纸 popover（网页贴纸不可点选），仅 U1 双端同步。

## P43 · §90 W 组 bug 修复 + R 组圆角/动效（2026-08-24）

审查文档 5 个 BUG 我逐条查证属实，非照抄结论。

| | 项 | 状态 |
|---|---|---|
| W1 | 🔴 ND-001 笔记加 `BackHandler`：此前顶栏返回走 `saveAndBack`、**系统返回直接 pop**，同一动作两套行为，从系统返回退出就吞内容 | [~] |
| W2 | 🔴 ND-002 网页全局搜索字段 `p.body` → `p.content`（笔记+日记）—— 搜的字段根本不存在，正文**从来搜不到** | [~] |
| W3 | ND-003 草稿恢复条件去掉 `d.length > content.length` —— 原条件让「改短/重写/清空」后永远恢复不了，那恰是最该救的几种 | [~] |
| W4 | ND-004 网页 `modal()` 遮罩点击加 dirty 判断，与 App 的 Discard 合同同语义 | [~] |
| W5 | ND-005「允许并继续」直接接着执行润色 —— 此前只设权限 + 提示「再点一次」 | [~] |
| W6 | ⏸ **待拍板**：两端主题门禁不一致。文档 §2.4 建议**去掉 Android 门禁**（基础自定义色免费），不是给网页加。是定价决策，两条路都要两端一致 | [ ] |
| R1 | 圆角归拢：`Shapes.extraLarge` 8→10；搜索框 44dp/22dp 胶囊 → **38dp/4dp**（按实机修订 §64 那条「输入框 22dp 胶囊」）；待办搜索条同步 | [~] |
| R2 | 动效：主题切换加 **300ms crossfade**（此前是硬切）；`rowClick` 按压加 80/120ms 过渡（此前瞬变） | [~] |

**更正记录**：§90 初稿 R2 表写「主题切换 600/650 → 360」是看 tween 直方图想当然 ——
实查那三处（650/1600/600）全是小鹿呼吸与打字点的**无限循环动画**，不是转场，不动；
真正的问题是 `Theme.kt` 没有任何 `animateColorAsState`，换主题是硬切。已在 §90 更正。

## P44 · §90 形态组：笔记清单改形态 + 搜索三态 + 日记按月分组（2026-08-24）

| | 项 | 状态 |
|---|---|---|
| N1 | 🔴 **撤掉自创的清单 chips 横排**（双端）—— Lifebear 笔记列表页没有它，切清单在编辑页 chip 下拉里做（V014 [B]）。清单数据、Room v7 迁移、编辑页的 ListChange/CreateList 全部保留 | [~] |
| S1 | 搜索框静置态：38dp 高 / 4dp 圆角 / placeholder 收成「正文」（实机只写「本文」两字）。双端 | [~] |
| S2 | tab 两个**等宽平分** + 选中态改**整宽 3dp 粗下划线**（原 22dp 短横是自创） | [~] |
| S3 | 🔴 **搜索激活态**（此前完全没有）：点搜索框 → 顶栏变「← + 裸输入框」自动聚焦、IME 为 Search 键、返回键退出并清空。placeholder 此时才写全「笔记名、正文」/「日记正文」 | [~] |
| N3 | 日记列表改**按月分组**：灰色月份小标题 + 每条「大号日期 26sp / 星期」竖排在左、正文在右、心情作右侧识别符。原「emoji + 日期 + 摘要」紧凑行是我们自己的排法 | [~] |

**未做（缺证据，不自创）**：Lifebear 的 ノート tab 列表页长什么样、清单在其中如何呈现 ——
V014 只录到 `← マイリスト ⋮` 那个二级页，没录到 tab 内的列表形态。笔记列表暂显示全部笔记，
不按清单收窄。要继续对齐得补一张 ノート tab 的实机图。

**挂账**：清单重命名/改色/删除/排序（审查文档指出的缺口，N2）未做。

---

## P45 · §93/§94/§95 三份对照与审计入计划（2026-08-24，只写计划未执行）

**本条只记录计划与文档校正，没有改任何功能代码。**

### 做了什么
- [x] §93 —— ノート&日記 实机 13 图（去重 7 张新图）对照，写入 `FEATURE-PLAN.md`
- [x] §94 —— ToDo 全流程实机 17 图对照，写入 `FEATURE-PLAN.md`
- [x] §95 —— `TASK-LIST-LIFECYCLE-AUDIT.md` 逐条代码复核，写入 `FEATURE-PLAN.md`
- [x] P4-5 拆成 P4-5a `[~]` / P4-5b `[ ]` —— 原 `[x]` 是虚标（网页无「已完成清单」）

### §93 撤回的自述（诚实记录）
上一批（P44）我写「实机 38dp 高」时**没有量过图**，实测是 43.8dp；
`TodoScreen.kt:88` 那条「与笔记页同规格（实机 38dp）」照抄了这个错值。
placeholder 也写错：实机 ノート tab 是「ノート名、本文」、日記 tab 是「本文」，**两 tab 不同**，
且激活态与静置态**同文案**；我把两 tab 都写成「正文」，还自创了「静置短/激活长」的规则。
日记列表右侧的心情 emoji 同样是自创 —— 实机列表不显示心情。

**P44 的 N1 判定要从「形态已改回」降级**：撤 chips 是对的，但落点错了 ——
实机 ノート tab 显示的是**清单列表**（两级结构），我做成了一级的笔记列表。

### §95 复核结论
审计文档 12 BUG + 2 RISK 中，13 条可静态验证的**全部成立**，无虚报。
唯一不能静态定论的是 TL-002 的真机时序。

### 待执行（均未开工）
- [ ] 批 TL-A（插队最先）：TL-001 上行分批确认 / TL-006 复合游标 / TL-002 del 分支加版本检查
- [ ] 批 E（§93）：E1–E8，笔记两级结构 + 搜索框实测值
- [ ] 批 F（§94）：F1–F9，ToDo 中枢页重排 + 子页快速添加行
- [ ] 批 TL-B / TL-C / TL-D：删除撤销合同 / 清单事务 / 建立与清除体验

### 待用户拍板
- 归档清单是否只读；归档任务是否进星标与未来 7 天
- 未来 7 天到底 7 天还是 8 天（建议对齐用户指南：7 天）
- 标签（ラベル）做不做；若做**建议免费**，不跟 Lifebear 的付费墙
- §93 W6 遗留：主题 Pro 门禁双端不一致，方向未定

### 已知缺证据
- Lifebear 清单**删除**入口：13 图里的 ⋮ 只有「編集/並び替え」两项，未见删除

## P46 · §96 五条口径实机裁决（2026-08-24，只写计划未执行）

### 已定案（可写进合同）
- [x] 归档清单 = **只读**，不能新增任务（用户实机确认）
- [x] 清单**允许重名**，靠颜色圆点区分（图 66 两个同名清单并存）
- [x] Lifebear **没有「清除已完成」动作** —— 用 `期間選択`（默认近 3 月）代替删除
- [x] 从「未来 7 天」快速添加，**日期自动填今天**（图 74/75）
- [x] 星标页是**快照式**：页内取消星标 / 标完成，条目**不消失**（图 78/79 对照）

### 仍缺证据，未定案
- [ ] #2 归档任务进不进星标 / 未来 7 天 —— 缺「归档前后首页计数 + 星标页」3 张对照图
- [ ] #3 未来 7 天是 7 天还是 8 天 —— 缺一张「今天+6 / 今天+7 两条任务」的边界图

### 待用户拍板
- [ ] 网页「清除已完成」按钮是否按 Lifebear 删掉（**这是删功能，执行前再确认**）
- [ ] 「不能建过期任务」是否对齐（**建议不对齐** —— 事后补录是真实场景）

### 新记的差距（不进批 F，另排）
- [ ] `タスク作成` 实机是**整页编辑器**（三组属性行 + 相机），我们是 `AlertDialog`
- [ ] `予定作成` 默认**精简态** + 「詳細を表示」展开；通知默认值随「終日」开关变
- [ ] 日程名右侧图标 → **テンプレート + 履歴** 两组；我们有模板，**没有「履歴」**
